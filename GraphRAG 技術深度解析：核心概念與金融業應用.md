# GraphRAG 技術深度解析：核心概念與金融業應用

**作者**：Manus AI  
**日期**：2026 年 1 月  
**主題**：Microsoft GraphRAG 的架構、核心概念與金融業實現方案

---

## 目錄

1. [GraphRAG vs 傳統 RAG：本質差異](#graphrag-vs-傳統-rag本質差異)
2. [核心概念解析](#核心概念解析)
3. [GraphRAG 如何判別節點關聯性](#graphrag-如何判別節點關聯性)
4. [金融業應用場景](#金融業應用場景)
5. [實施建議](#實施建議)

---

## GraphRAG vs 傳統 RAG：本質差異

### 傳統 RAG 的工作原理與局限

傳統的檢索增強生成（Retrieval-Augmented Generation, RAG）系統通過向量相似性搜索來檢索相關信息。系統首先將文檔分割成小的文本塊（通常 300-500 個 token），然後將這些塊轉換為向量嵌入，存儲在向量數據庫中。當用戶提出查詢時，系統將查詢轉換為向量，並找到語義上最相似的文本塊，將其添加到 LLM 的上下文中以生成回答。

這種方法在處理簡單的事實查詢時表現良好，但存在幾個根本性的限制。首先，**分塊過程破壞了文檔的原始結構**。當文本被分割成孤立的片段時，段落之間的邏輯流、層級關係和上下文連接都會丟失。其次，**傳統 RAG 缺乏推理能力**。系統只能根據語義相似性檢索文本，無法理解概念之間的因果關係或邏輯連接。第三，**多跳推理困難**。如果答案需要連接多個概念（例如 A 導致 B，B 導致 C），而 B 在查詢中沒有直接提及，系統可能無法檢索到 B，從而破壞整個推理鏈。最後，**全局查詢能力弱**。對於需要綜合分析整個語料庫的問題（如「這些文檔中的主要趨勢是什麼？」），傳統 RAG 只能檢索相似的片段，無法進行語料庫級別的分析。

### GraphRAG 的創新方案

GraphRAG 通過根本性地改變數據表示方式來解決這些問題。與其將文檔表示為孤立的向量塊，GraphRAG 構建一個**知識圖譜**，其中實體成為節點，實體之間的關係成為邊。這種圖結構保留了原始文檔中的所有連接信息。

GraphRAG 的核心優勢包括：**結構保留**。通過將實體表示為節點，將關係表示為邊，知識圖譜保留了文檔中的所有結構信息。**多跳推理**。系統可以通過圖遍歷實現多跳推理。如果要回答涉及 A→B→C 的問題，系統可以直接沿著圖中的邊進行遍歷，而不依賴於語義相似性。**全局查詢能力**。GraphRAG 使用社區檢測算法將相關實體分組，並為每個社區生成摘要。對於全局查詢，系統可以使用 Map-Reduce 模式並行處理所有社區摘要，而不是檢索大量孤立的文本塊。**靈活的檢索機制**。GraphRAG 支持多種檢索方式，包括語義搜索、文本搜索、地理空間過濾等，不僅限於向量相似性。

下表總結了兩種方法的關鍵區別：

| 特性 | 傳統 RAG | GraphRAG |
|------|---------|----------|
| **數據表示** | 孤立的文本塊向量 | 知識圖譜（節點和邊） |
| **結構保留** | 否（分塊時丟失） | 是（通過邊保留） |
| **檢索機制** | 向量相似性搜索 | 語義、文本、混合等多種方式 |
| **全局查詢** | 差（無法綜合） | 優（Map-Reduce 社區摘要） |
| **多跳推理** | 有限（獨立檢索） | 原生支持（圖遍歷） |
| **索引成本** | 低（僅嵌入） | 高（LLM 提取+聚類） |
| **查詢成本** | 低 | 變化（本地查詢便宜，全局查詢昂貴） |
| **最適用場景** | 事實查詢、小規模語料庫 | 主題分析、複雜推理、大規模語料庫 |

---

## 核心概念解析

### Entity（實體）

在 GraphRAG 中，**Entity 是從文本中提取的具體概念**。實體提取是索引管道的第一步，使用 LLM 根據精心設計的提示詞從文本塊中識別和提取實體。

實體的類型包括：**人物**（如「張三」、「王五」）、**組織**（如「國泰銀行」、「中國銀行」）、**地點**（如「台北」、「上海」）、**產品或服務**（如「信用貸款」、「房屋貸款」）、**事件**（如「降息」、「政策調整」）、**概念或主題**（如「金融風險」、「信用評分」）。

實體提取的過程涉及 LLM 對每個文本塊進行分析，識別其中包含的所有相關概念。例如，如果文本塊包含「國泰銀行於 2024 年推出了新的信用貸款產品，利率為 3.5%」，LLM 會提取以下實體：國泰銀行、信用貸款、2024 年、3.5%。

### Node（節點）

**Node 是知識圖譜中代表實體的結構化單位**。一旦實體被提取並放入知識圖譜中，它就成為一個節點。Node 和 Entity 之間的關鍵區別在於：Entity 是提取出的原始概念，而 Node 是在圖結構中的表示，具有額外的屬性和上下文信息。

每個節點包含以下信息：**節點 ID**（唯一標識符）、**節點名稱**（實體的名稱）、**節點類型**（實體的分類，如「銀行」、「產品」、「事件」）、**節點描述**（從相關文本中生成的節點描述）、**節點屬性**（其他相關屬性，如利率、風險等級等）。

在 GraphRAG 的圖構建階段，系統會進行**實體去重**，將指向同一概念的多個表述合併為單一節點。例如，「國泰銀行」、「國泰」、「國銀」可能都指向同一個節點。這確保了圖的一致性和效率。

### Relationship（關係）

**Relationship 是連接兩個節點的邊，表示實體之間的語義或邏輯連接**。每條邊都有一個類型標籤，表示關係的性質。

常見的關係類型包括：**所有關係**（如「銀行擁有分支」）、**提供關係**（如「銀行提供貸款」）、**影響關係**（如「政策影響利率」）、**參與關係**（如「客戶申請貸款」）、**從屬關係**（如「分支屬於銀行」）。

在金融領域，一個典型的關係網絡可能如下所示：

```
申請人 --[申請]--> 信用貸款
申請人 --[有]--> 信用評分
信用評分 --[影響]--> 貸款批准
信用貸款 --[提供方]--> 銀行
銀行 --[受監管於]--> 金融監管政策
```

### Hop（跳躍）

**Hop 是指在知識圖譜中從一個節點遍歷到另一個節點所經過的邊數**。這個概念對於理解多跳推理至關重要。

- **1-Hop**：直接相連的兩個節點之間的路徑。例如，「國泰銀行」直接連接到「信用貸款」。
- **2-Hop**：相隔一個中間節點的路徑。例如，「申請人」→「信用評分」→「貸款批准」。
- **3-Hop 及以上**：更複雜的多步路徑。例如，「申請人」→「信用貸款」→「銀行」→「金融監管政策」。

Hop 的數量直接影響查詢的複雜性和計算成本。對於簡單的查詢，通常只需要 1-2 跳；對於複雜的分析，可能需要 3-5 跳或更多。

### 三者的關係

Entity、Node 和 Hop 構成了 GraphRAG 的基本概念框架。實體是原始的提取物，節點是圖中的表示，而跳躍是在圖中進行推理的方式。理解這三個概念的關係對於有效使用 GraphRAG 至關重要。

---

## GraphRAG 如何判別節點關聯性

### 實體和關係提取階段

GraphRAG 的第一步是從文本中提取實體和關係。這個過程完全由 LLM 驅動，使用精心設計的提示詞。提示詞指導 LLM 識別文本中的所有相關實體，並提取它們之間的關係。

提取的結果是一組三元組（triple），格式為 (主體, 關係, 客體)。例如：

- (國泰銀行, 提供, 信用貸款)
- (信用貸款, 有, 利率 3.5%)
- (申請人, 申請, 信用貸款)

LLM 不僅提取實體和關係，還提供每個實體和關係的描述，這些描述後來用於生成社區摘要和查詢回答。

### 圖構建和實體去重

一旦提取了所有三元組，系統就開始構建知識圖譜。在這個階段，系統需要處理一個重要的挑戰：**實體去重**。由於 LLM 可能以不同的方式表述同一實體（例如「國泰銀行」vs「國泰」），系統需要識別並合併這些重複的實體。

GraphRAG 使用多種策略進行實體去重，包括**精確匹配**（完全相同的字符串）、**相似性匹配**（使用編輯距離或其他相似性指標）、**語義匹配**（使用嵌入向量進行語義相似性比較）。

在實體去重後，系統構建圖結構，其中每個唯一的實體成為一個節點，每個三元組成為一條邊。如果多個三元組表示相同的關係（例如來自不同的文本塊），系統會合併這些邊，可能創建一個包含所有來源信息的聚合邊。

### 社區檢測和層級結構

實體和關係提取完成後，GraphRAG 應用社區檢測算法（通常是 Leiden 算法）來識別圖中的社區。社區是一組高度相互連接的節點，代表相關的實體集群。

社區檢測產生一個**層級結構**。在最細粒度的層級（層級 0），社區很小，可能只包含直接相關的實體。隨著層級增加，社區變得更大，包含更廣泛的相關實體。例如：

- **層級 0**：「信用貸款」及其直接相關的屬性和申請者
- **層級 1**：「國泰銀行」及其所有產品和服務
- **層級 2**：整個「金融行業」的概況

### 社區摘要生成

對於每個社區，GraphRAG 生成一個文本摘要。這些摘要由 LLM 根據社區中的所有節點和邊生成，捕捉社區的主要主題和關鍵信息。

社區摘要的生成過程涉及：

1. **信息收集**：收集社區中所有節點的描述和所有邊的信息
2. **摘要生成**：使用 LLM 根據收集的信息生成簡潔的文本摘要
3. **層級摘要**：對於多層級社區，系統生成多個層級的摘要，從詳細到高層次

### 節點關聯性的多維度判別

GraphRAG 通過多種方式判別節點之間的關聯性：

**直接連接**：通過邊直接連接的節點被認為高度相關。邊的類型進一步指定了關聯的性質。

**共同鄰居**：共享相同鄰居的節點被認為間接相關。例如，如果兩個貸款產品都由同一銀行提供，它們通過銀行節點相關聯。

**路徑相似性**：通過相似的邊序列連接的節點被認為結構相似。例如，兩個具有相同的「提供方」→「監管機構」路徑的產品被認為在監管方面相似。

**社區成員**：同一社區中的節點被認為在語義上相關，即使它們沒有直接連接。這是因為社區檢測算法識別了高度相互連接的節點集群。

**邊的權重和屬性**：根據邊出現的頻率、來源的可靠性或其他因素，邊可能被賦予不同的權重。更高權重的邊表示更強的關聯。

### 查詢時的關聯性利用

在查詢時，GraphRAG 利用這些關聯性來檢索相關信息。對於本地查詢（針對特定實體的問題），系統使用圖遍歷來收集相關的節點和邊。對於全局查詢（需要語料庫級別分析的問題），系統使用社區摘要和 Map-Reduce 模式來聚合信息。

---

## 金融業應用場景

### 場景 1：信用貸款風險評估

#### 問題陳述

金融機構需要快速、準確地評估信用貸款申請的風險。傳統的 RAG 系統可能會檢索到關於申請人信用評分的信息和關於銀行風險政策的信息，但無法有效地連接這些信息來進行綜合風險評估。

#### GraphRAG 解決方案

GraphRAG 可以構建一個包含以下實體和關係的知識圖譜：

**關鍵實體**：申請人、信用評分、貸款產品、銀行、擔保物、信用歷史、收入信息、負債信息、風險政策、監管要求。

**關鍵關係**：

- 申請人 --[申請]--> 信用貸款
- 申請人 --[有]--> 信用評分
- 申請人 --[有]--> 收入信息
- 申請人 --[有]--> 負債信息
- 信用評分 --[影響]--> 貸款批准
- 信用評分 --[基於]--> 信用歷史
- 信用貸款 --[受限於]--> 風險政策
- 銀行 --[制定]--> 風險政策
- 銀行 --[受監管於]--> 監管要求

**多跳推理示例**：

當系統收到查詢「張三申請信用貸款的風險如何？」時，它可以進行以下推理：

1. 找到申請人節點「張三」
2. 通過「申請」邊找到「信用貸款」節點
3. 通過「有」邊找到張三的「信用評分」
4. 通過「影響」邊確定信用評分對批准的影響
5. 通過「受限於」邊找到適用的「風險政策」
6. 通過「受監管於」邊找到相關的「監管要求」
7. 綜合所有信息進行風險評估

這種多跳推理方式確保了評估的全面性和準確性。

### 場景 2：供應鏈金融

#### 問題陳述

在供應鏈金融中，金融機構需要理解複雜的供應商-核心企業-客戶網絡，並根據這個網絡的結構進行融資決策。傳統 RAG 無法有效地表示和分析這種複雜的網絡結構。

#### GraphRAG 解決方案

GraphRAG 可以構建一個供應鏈知識圖譜，其中：

**關鍵實體**：核心企業、供應商、客戶、採購訂單、發票、融資產品、支付條款、信用評級。

**關鍵關係**：

- 供應商 --[供應]--> 核心企業
- 核心企業 --[銷售]--> 客戶
- 核心企業 --[發起]--> 採購訂單
- 採購訂單 --[對應]--> 發票
- 發票 --[關聯]--> 融資產品
- 融資產品 --[提供]--> 銀行
- 供應商 --[有]--> 信用評級
- 核心企業 --[有]--> 信用評級

**應用示例**：

當系統收到查詢「供應商 A 的融資風險如何？」時，它可以進行以下分析：

1. 找到供應商 A 節點
2. 通過「供應」邊找到所有依賴供應商 A 的核心企業
3. 對於每個核心企業，通過「銷售」邊找到其客戶
4. 追蹤採購訂單和發票的流向
5. 評估整個供應鏈的信用風險
6. 根據多層級社區結構評估系統性風險

這使得金融機構能夠進行深層次的供應鏈分析，而不僅僅是評估單個參與者。

### 場景 3：監管合規和風險管理

#### 問題陳述

金融機構需要快速識別與特定交易或客戶相關的所有監管要求和風險管理政策。這涉及複雜的層級結構和多個相互關聯的政策。

#### GraphRAG 解決方案

GraphRAG 可以構建一個監管和風險管理知識圖譜，其中：

**關鍵實體**：交易類型、客戶類型、監管機構、監管政策、風險類別、風險控制措施、合規檢查清單。

**關鍵關係**：

- 交易類型 --[受監管於]--> 監管政策
- 客戶類型 --[受監管於]--> 監管政策
- 監管政策 --[涉及]--> 風險類別
- 風險類別 --[由]--> 風險控制措施
- 風險控制措施 --[包含]--> 合規檢查清單

**應用示例**：

當系統收到查詢「進行跨境匯款需要哪些合規檢查？」時，它可以：

1. 找到「跨境匯款」交易類型節點
2. 通過「受監管於」邊找到所有適用的「監管政策」
3. 對於每個政策，通過「涉及」邊找到相關的「風險類別」
4. 對於每個風險類別，通過「由」邊找到「風險控制措施」
5. 對於每個控制措施，通過「包含」邊找到具體的「合規檢查清單」

系統可以自動生成一份完整的合規檢查清單，確保沒有遺漏任何要求。

---

## 實施建議

### 數據準備

在實施 GraphRAG 之前，金融機構應該：

1. **評估數據質量**：確保輸入文本的質量高，避免噪音和重複。金融文檔通常包含結構化和非結構化數據的混合，需要適當的預處理。

2. **定義實體和關係類型**：明確定義金融領域中相關的實體類型（如「銀行」、「貸款產品」、「客戶」等）和關係類型（如「提供」、「申請」、「受監管於」等）。這可以通過領域專家的輸入來完成。

3. **準備訓練數據**：為了優化 LLM 的實體和關係提取，應該準備一些標註的訓練數據，用於 GraphRAG 的提示詞調整。

### 提示詞優化

GraphRAG 的性能高度依賴於提示詞的質量。金融機構應該：

1. **使用 prompt-tune 功能**：GraphRAG 提供了自動提示詞調整功能，可以根據特定的數據集自動生成最優的提示詞。

2. **手動調整提示詞**：根據金融領域的特定需求，手動調整提示詞以提高實體和關係提取的準確性。

3. **測試和驗證**：在大規模部署之前，對提示詞進行充分的測試和驗證。

### 配置優化

1. **調整分塊大小**：金融文檔可能有特殊的結構（如表格、列表等），需要根據這些特性調整分塊大小。

2. **配置社區層級**：根據金融數據的複雜性，配置適當數量的社區層級。更多的層級提供更細粒度的分析，但會增加計算成本。

3. **選擇合適的 LLM**：不同的 LLM 在實體提取和推理方面的性能不同。金融機構應該測試不同的模型，選擇最適合其需求的模型。

### 成本管理

GraphRAG 的成本主要來自 LLM 的 API 調用。為了管理成本：

1. **批量處理**：使用批量 API 調用來減少成本。

2. **使用本地 LLM**：考慮使用本地部署的 LLM（如 Ollama）來減少 API 成本。

3. **優化查詢**：設計高效的查詢策略，避免不必要的多跳推理。

### 監控和評估

1. **建立評估指標**：定義用於評估 GraphRAG 性能的指標，如檢索準確性、查詢響應時間、成本等。

2. **定期審查**：定期審查系統的性能，並根據反饋進行調整。

3. **用戶反饋**：收集用戶反饋，了解系統是否滿足實際業務需求。

---

## 結論

GraphRAG 代表了 RAG 技術的重要進步，特別是在處理複雜、相互關聯的數據時。在金融業中，GraphRAG 的多跳推理能力、社區檢測功能和層級結構使其特別適合於風險評估、供應鏈分析和監管合規等複雜任務。

通過正確的實施和優化，GraphRAG 可以幫助金融機構構建更強大、更可靠的 AI 系統，從而改善決策質量和降低風險。

---

## 參考資源

[1] Memgraph. "RAG vs GraphRAG: Shared Goal & Key Differences" - https://memgraph.com/blog/rag-vs-graphrag

[2] Neo4j. "What Is GraphRAG?" - https://neo4j.com/blog/genai/what-is-graphrag/

[3] DataCamp. "GraphRAG: Graph-Based Retrieval-Augmented Generation" - https://www.datacamp.com/tutorial/graphrag

[4] Microsoft. "GraphRAG Documentation" - https://microsoft.github.io/graphrag/

[5] Memgraph. "What is GraphRAG? A Guide to Connected Context" - https://memgraph.com/blog/what-is-graphrag

[6] Medium. "How Microsoft GraphRAG Works Step-By-Step (Part 1/2)" - https://tech.bertelsmann.com/en/blog/articles/how-microsoft-graphrag-works-step-by-step-part-12
